# Server networking
PORT=8080
HOST=0.0.0.0

# Storage (leave blank to use system temp dir automatically)
TEMP_DIR=
AUDIO_TTL_SECONDS=3600

# TTS defaults
DEFAULT_VOICE=en-IN-PrabhatNeural
DEFAULT_RATE=+0%
DEFAULT_PITCH=+0Hz
DEFAULT_VOLUME=+0%

# Misc
MAX_LOG_ENTRIES=1000
